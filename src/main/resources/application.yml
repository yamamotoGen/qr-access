spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/user_identity_db
    username: Maksim
    password: Maksimpass
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    properties:
      hibernate:
        format-sql: true
